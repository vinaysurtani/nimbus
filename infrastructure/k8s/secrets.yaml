# DO NOT commit real values here — this file is a template.
# Generate base64 values with: echo -n 'your-value' | base64
# Store real secrets in your cluster using:
#   kubectl create secret generic nimbus-secrets --from-literal=... -n nimbus
apiVersion: v1
kind: Secret
metadata:
  name: nimbus-secrets
  namespace: nimbus
type: Opaque
data:
  # Replace with: echo -n 'your-password' | base64
  postgres-password: Y2hhbmdlX21l
  # Replace with: echo -n 'postgres://user:your-password@postgres:5432/nimbus?sslmode=disable' | base64
  postgres-url: Y2hhbmdlX21l
  # Replace with: echo -n 'sk-ant-your-key' | base64
  anthropic-api-key: Y2hhbmdlX21l
